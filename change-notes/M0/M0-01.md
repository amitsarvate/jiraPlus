# M0-01: Initialize repo structure
- Set up npm workspaces monorepo with root `package.json` and shared tooling (TypeScript, ESLint/Prettier).
- Added base configs: `tsconfig.base.json`, `.editorconfig`, `.gitignore`.
