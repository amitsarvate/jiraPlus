# M1-03: Frontend Connect Jira button
- Added client-side “Connect Jira” button that redirects to backend `/auth/jira/start`.
- Ensured page runs as a Client Component to allow click handler.
- Updated frontend lint config/script to avoid Next.js lint circular config issue in workspaces (using eslint + @typescript-eslint instead of next/core-web-vitals).
